import { createTheme } from "@mui/material";

const theme = createTheme({
    cssVariables: true,
    palette: {
        background: {
            default: "#155797",
        },
        primary: {
            main: "#ffa500",
        },
    },
})

export default theme;